# Logging Middleware

This module provides logging functionality for the application.

It sends structured logs to an external logging service using HTTP requests.

## Features
- Supports log levels (info, error, debug)
- Non-blocking logging (fails silently)
- Can be reused across services